There are <%= ((@stores.count) +1) %> stores in your search

<% @stores.each do |store| %>
<%= store.name %></br>
<%= store.city %></br>
<%= store.distance %></br>
<%= store.phone %></br>
<%= store.type %></br>
</br>
<% end %>
